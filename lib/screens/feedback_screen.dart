import 'package:flutter/material.dart';

class FeedbackScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Kritik & Saran")),
      body: Center(
        child: Text("Formulir Google diintegrasikan di sini."),
      ),
    );
  }
}
