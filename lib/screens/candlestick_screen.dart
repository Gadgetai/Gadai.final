import 'package:flutter/material.dart';

class CandlestickScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Analisa Candlestick")),
      body: Center(
        child: Text("Analisa grafik dan berita terkait crypto/forex."),
      ),
    );
  }
}
